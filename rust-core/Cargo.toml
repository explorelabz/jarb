[package]
name = "hedge-core"
version = "0.1.0"
edition = "2021"

[lib]
name = "hedge_core"
crate-type = ["cdylib"]

[dependencies]
pyo3 = { version = "0.24", features = ["extension-module", "abi3-py312"] }

[profile.release]
lto = "fat"
codegen-units = 1
panic = "abort"
strip = "symbols"
